{% extends "layout.njk" %}

{% block head %}
    <title>Receita - {{item.title}}</title>
{% endblock head %}

{% block style %}
    <link rel="stylesheet" href="/styles/style-recipe.css">
{% endblock  style%}

{% block content %}
<div class="container">

    <div class="preview">
        <img src="{{item.image}}" alt="{{item.title}}">
        <h1>{{item.title}}</h1>
        <h3>Por {{item.author}}</h3>
    </div>

    <div class="info_container">
        <div class = "title">
            <h2>Ingredientes</h2>
            <a class = "hide_show">ESCONDER</a>
        </div>
        <div class = "item_info visible">
            <ul>
            {% for ingredient in item.ingredients%}
                <li>{{ingredient}}</li>
            {% endfor %}
            </ul>
        </div>
    </div>

    <div class = "info_container">
        <div class = "title">
            <h2>Modo de Preparo</h2>
            <a class = "hide_show">ESCONDER</a>
        </div>
        <div class = "item_info visible">
            <ul>
            {% for preparation in item.preparation %}
                <li>{{preparation}}</li>
            {% endfor %}
            </ul>  
        </div>
    </div>

    <div class = "info_container">
        <div class = "title">
            <h2>Informações Adicionais</h2>
            <a class = "hide_show">ESCONDER</a>
        </div>
        <div class = "item_info visible">
            <p>{{item.information}}</p>
        </div>
    </div>

</div>
{% endblock content%}

{% block scripts %}
    <script src="scripts/scripts-recipes.js"></script>
{% endblock scripts %}